rename-session " "

# pass
new-window -k -t0 -n " "
send-keys -R ' unset HISTFILE' 'Enter' 'gpgssh' 'Enter' 'c' 'Enter'

# caffeination area
new-window 'tmux-caffeinate-banner; $SHELL'
send-keys -R 'caffeinate' 'Enter'

# mpsyt
new-window -n "HÖR"
send-keys -R ' unset HISTFILE' 'Enter' 'mpsyt h' 'Enter'
# bind-key -n F7 run-shell ' tmux send-keys -t " :3.0" "<"'
# bind-key -n F8 run-shell ' tmux send-keys -t " :3.0" " "'
# bind-key -n F9 run-shell ' tmux send-keys -t " :3.0" ">"'
# bind-key < run-shell ' tmux send-keys -t " :3.0" "Left"'
# bind-key > run-shell ' tmux send-keys -t " :3.0" "Right"'
